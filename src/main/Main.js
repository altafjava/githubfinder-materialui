import React, { Fragment } from 'react';
import Header from '../components/layout/Header';

const Main = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default Main;
